# Copy to .env and fill in. Both values come from your Sanity project at sanity.io/manage.
# Use the SAME project id + dataset in frontend/.env.local.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
